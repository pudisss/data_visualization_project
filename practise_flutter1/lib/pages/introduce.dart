import "package:flutter/material.dart";

void main() => runApp(introduce());

class introduce extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold());
  }
}
